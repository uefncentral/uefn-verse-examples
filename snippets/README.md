# Snippets — digest-validated reference fragments

These files are **real Verse code validated against Epic's official API digest**, but they are *fragments of larger projects*: they reference classes, assets, or managers defined in the original creator's other files, so they **do not compile as standalone single files**.

Use them as reference for patterns and API usage. For **copy-paste-ready code that compiles as-is**, see [`/examples`](../examples/) — every file there passed a real UEFN Verse build.

Want a self-contained version of any pattern here? [Describe it in one sentence and our generator writes it](https://uefncentral.com/generator?ref=github) — checked against the same official digest.
